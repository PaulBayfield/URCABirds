# Worker settings
API_URL=
API_KEY=
SENSOR_ID=
SENSOR_NAME=        # Optional: human-readable sensor name (defaults to SENSOR_ID)
CONFIDENCE_THRESHOLD=
LATITUDE=
LONGITUDE=
LOG_LEVEL=INFO          # Optional: DEBUG, INFO, WARNING, ERROR (default: INFO)

# API settings
API_DOMAIN=

# PostgreSQL
POSTGRES_DATABASE=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
