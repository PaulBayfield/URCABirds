[project]
name = "urcabirds"
version = "1.0.0"
description = "URCABirds Project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "audiomentations>=0.43.1",
    "matplotlib>=3.10.8",
    "plotly>=6.6.0",
]

[tool.uv.workspace]
members = [
    "src/api",
    "src/dashboard",
]
