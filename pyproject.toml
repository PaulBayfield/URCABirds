[project]
name = "urcabirds"
version = "1.0.0"
description = "URCABirds Project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "src/api",
    "src/worker",
]
